protocol_version: 1
sample_interval_s: 900
arc_duration_s: 86400
spacecraft:
  # ESA describes the three spacecraft as identical, approximately 500 kg.
  # Exact attitude-dependent projected area is unavailable in the orbit file;
  # therefore area is explicitly estimated as an effective ballistic parameter.
  mass_kg: 468.0
  drag_coefficient: 2.2
  nominal_drag_area_m2: 1.0
  calibration_area_m2: 4.0
  srp_area_m2: 1.0
  reflectivity_coefficient: 1.3
acceptance:
  validation_rms_improvement_min_fraction: 0.05
  storm_test_rms_degradation_max_fraction: 0.10
  fitted_drag_area_bounds_m2: [0.05, 8.0]
